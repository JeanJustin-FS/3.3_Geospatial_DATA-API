# Geospatial Data API

## 🚀 Overview
A Node.js API that fetches geospatial data from an external source, stores it in MongoDB, and provides RESTful endpoints for retrieval.

## 📌 Setup
1. Clone this repository.
2. Run `npm install` to install dependencies.
3. Create a `.env` file with MongoDB and API credentials.
4. Start the server using `npm run dev`.

## 📡 Endpoints
- `GET /api/geo-data?lat={}&lon={}` – Fetch data from API
- `POST /api/geo-data` – Save data to MongoDB
- `GET /api/geo-data/all` – Retrieve all stored data
- `GET /api/geo-data/:id` – Get data by ID